name: OWASP Security Pipeline

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  actions: read

jobs:
  dependency_check:
    name: SCA - OWASP Dependency-Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Crear carpeta de reportes
        run: mkdir -p reports/dependency-check

      - name: Run Dependency-Check
        uses: dependency-check/Dependency-Check_Action@main
        with:
          project: 'mi-proyecto'
          path: '.'
          format: 'HTML'
          out: 'reports/dependency-check'
          args: >
            --noupdate

      - name: Ver archivos generados por Dependency-Check
        if: always()
        run: |
          echo "Contenido de reports:"
          ls -R reports || true

      - name: Subir reporte Dependency-Check
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: reports/dependency-check

  zap_baseline:
    name: DAST - OWASP ZAP Baseline
    runs-on: ubuntu-latest
    needs: dependency_check

    steps:
      - name: Crear carpeta ZAP
        run: mkdir -p reports/zap

      - name: Run ZAP Baseline
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: 'https://tusitio.com'
          fail_action: false
          cmd_options: '-a -r reports/zap/zap-report.html -w reports/zap/zap-report.md -J reports/zap/zap-report.json'

      - name: Ver archivos generados por ZAP
        if: always()
        run: |
          echo "Contenido de reports:"
          ls -R reports || true

      - name: Subir reporte ZAP
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-baseline-report
          path: reports/zap
